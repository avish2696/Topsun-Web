import type { Config } from 'tailwindcss'

const config = {
  // Tailwind v4 uses @source directive in CSS instead of content array
  // See src/styles/tailwind.css for @source configuration
} satisfies Config

export default config

